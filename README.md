# Backlog

Backlog is a calm, mobile-friendly daily task tracker. It keeps each day's tasks separate, shows daily completion stats, and stores everything in the browser with `localStorage`.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
